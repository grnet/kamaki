#!/usr/bin/make -f

%:
	dh $@ --with python2

override_dh_auto_build:
	cd docs && make man && make text && rm -r _build/doctrees && rm -r _build/text/man

override_dh_installman:
	dh_installman docs/_build/man/kamaki.1

